var app = angular.module("listless");

app.controller("Home", ["$scope", "$state", "User", function($scope, $state, User){
	/*User.findMe(function(data){
		if(data.username){
			$state.go("user", {id : data.id});
		}
	});*/
}]);